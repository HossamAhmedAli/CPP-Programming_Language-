/*
    write a program to compute the area of triangle
*/
#include<stdio.h>
void main(void)
{
    float base,height;
    printf("Enter base: ");
    scanf("%f", &base);
    printf("Enter height: ");
    scanf("%f", &height);
    printf("Area of circle = %.2f", 0.5*base*height);
}
